--- a/nvidia-oot/drivers/media/platform/tegra/camera/vi/channel.c
+++ b/nvidia-oot/drivers/media/platform/tegra/camera/vi/channel.c
@@ -2079,16 +2079,29 @@
 
 	sd->grp_id = grp_id;
 	chan->grp_id = grp_id;
-	index = pad->index - 1;
-	while (index >= 0) {
-		pad = &entity->pads[index];
-		if (!(pad->flags & MEDIA_PAD_FL_SINK))
+	/*
+	 * Walk upstream through the chain by following each entity's sink
+	 * pad. The historical walk assumed the sink pad sits at
+	 * (source_pad_index - 1), which breaks for subdevs that order their
+	 * source pad first (e.g. GMSL serdes channel subdevs with source=0,
+	 * sink=1) and silently truncated the chain before the sensor.
+	 */
+	while (1) {
+		struct media_pad *sink_pad = NULL;
+
+		for (index = 0; index < (int)entity->num_pads; index++) {
+			if (entity->pads[index].flags & MEDIA_PAD_FL_SINK) {
+				sink_pad = &entity->pads[index];
+				break;
+			}
+		}
+		if (!sink_pad)
 			break;
 
 #if defined(NV_MEDIA_ENTITY_REMOTE_PAD_PRESENT) /* Linux 6.0 */
-		pad = media_entity_remote_pad(pad);
+		pad = media_entity_remote_pad(sink_pad);
 #else
-		pad = media_pad_remote_pad_first(pad);
+		pad = media_pad_remote_pad_first(sink_pad);
 #endif
 		if (pad == NULL || !tegra_is_v4l2_subdev(pad->entity))
 			break;
@@ -2106,8 +2119,6 @@
 			"vi-output", sd->name);
 		if (len < 0)
 			return -EINVAL;
-
-		index = pad->index - 1;
 	}
 	spec_bar(); /** for num_sd < MAX_SUBDEVICES */
 
